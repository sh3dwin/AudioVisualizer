<Window x:Class="AudioVisual.View.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:audioVisual="clr-namespace:AudioVisual"
        xmlns:viewModel="clr-namespace:AudioVisual.ViewModel"
        mc:Ignorable="d"
        Title="MainView" Height="450" Width="800">
    <Window.Resources>
        <DataTemplate DataType="{x:Type viewModel:LoopbackViewModel}">
            <audioVisual:LoopbackView/>
        </DataTemplate>
    </Window.Resources>
    <Grid>
        <ContentControl Content="{Binding LoopbackViewModel}"/>
    </Grid>
</Window>